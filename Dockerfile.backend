FROM node:20-alpine

WORKDIR /app

COPY backend/package*.json ./
RUN npm ci

COPY backend/ ./
RUN npm run build

EXPOSE 5000
ENV PORT=5000
ENV NODE_ENV=production

# Nota: con rootDir en ./ la estructura es dist/src/index.js
CMD ["node", "dist/src/index.js"]